# rms

Rename My Schema

```
Usage of rms
  -from string
        from schema name
  -host string
        host (default "localhost")
  -password string
        password
  -port int
        port (default 3306)
  -to string
        to schema name
  -user string
        username
```
